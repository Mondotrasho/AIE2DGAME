#include "SteeringBehaviour.h"
#include "VelocityAgent.h"
#include "Object.h"


Vector2 SteeringBehaviour::update(Object* agent, float deltaTime)
{
	////update(agent, deltaTime);
	//agent->set_velocity(Vector2(1,0));
	//auto a = agent->get_velocity();
	return Vector2();
}
